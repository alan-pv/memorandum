# Copy to /etc/default/duckdns (chmod 600) and fill in.
# The token is a password: it must never reach the repository.
DUCKDNS_DOMAIN=your-subdomain
DUCKDNS_TOKEN=00000000-0000-0000-0000-000000000000
